package animalKing;

import java.util.*;

public class Main
{
	public static ArrayList<Animals> filteredList = new ArrayList<>();

	// public static void printAniamls(ArrayList<Animals> animals, CheckAnimals tester)
	// {
	// 	filteredList.clear();

	// 	for (Animals a : animals)
	// 	{
	// 		if (tester.test(a))
	// 		{
	// 			System.out.println(v.getName() + " " + v.getFood());
	// 			filteredList.add(a);
	// 		}
	// 	}
	// }
}